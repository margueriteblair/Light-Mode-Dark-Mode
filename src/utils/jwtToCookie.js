//with a static method, it's to be called on a class as a whole, not just an instance of the class
//loool that's why it looks like java silly!!

export default class Cookies {
    //add static methods for cookies here
    
}